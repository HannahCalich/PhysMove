#' Example output from TurningAngles to demonstrate the PlotAngles function
#'
#' Output from \code{\link{TurningAngles}} function calculated using the tracks dataset and \code{\link{TurningAngles}} with max_hr = 120. Note that the TurningAngles
#' default is normally max_hr=240; however, this creates an unnecessarily large file for an example dataset so max_hr = 120 was used instead.
#'
#' @format List containing 10 list elements, 1 for each time window set using \code{\link{TurningAngles}} default settings
"angleList"
