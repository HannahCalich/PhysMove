#' Example output from FitDist function
#'
#' Output from \code{\link{FitDist}} function using tracks dataset and \code{\link{FitDist}} default parameters. First list element contains a dataframe of results output from \code{\link{FitDist}}
#' and the second list element is a record of if the data were normalized or not, which is needed for \code{\link{CompDist}} and \code{\link{PlotDist}}).
#'
#' @format list
"distResults"
