#' Example output from FitDist()
#'
#' Examples output from the \code{\link{FitDist}} function. Full distribution fits calculated using the \code{\link{tracks}}
#' dataset and FitDist() with full=TRUE. First list element contains a dataframe of distribution results (d min, parameters, etc)
#' and the second list element is a record of if the data were normalized or not, which is needed for \code{\link{CompDist}}
#' and \code{\link{PlotDist}}).
#'
#' @format list
"distResultsAll"
