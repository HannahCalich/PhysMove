#' Example output from fitDist() using the best-fit x min for an exponential distribution
#'
#' Example output from the \code{\link{fitDist}} function. Distribution fits calculated using the \code{\link{tracks}} dataset
#' and FitDist() with set_xmin=1.649160, which is the best-fit x min for an exponential distribution for the \code{\link{tracks}} dataset.
#' First list element contains a dataframe of distribution results (x min, parameters, etc) and the second list element is a record
#' of if the data were normalized or not, which is needed for \code{\link{compDist}} and \code{\link{plotDist}}).
#'
#' @format list
"distResultsExp"
